# Adelaide Pavilion - Sanity CMS

## Setup Complete ✅

Content has been migrated to Sanity. To update content:

### Option 1: Use Sanity Studio

```bash
cd sanity
npm install
npx sanity deploy
```

Then edit content at your deployed Studio URL.

### Option 2: Edit JSON files

Content is still available in `_data/*.json` if you want to re-migrate after changes.

### Project ID
`bt9gto0j` - Already configured in `js/cms-loader-sanity.js`

### Dataset
`production`